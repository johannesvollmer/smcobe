
# Setup
- `npm install`
- `npm install nodemon -g`

## Run
- `npm run build` or `npm run build:win` to build once 
- `npm run serve` or `npm run serve:win` to launch and re-build on source file change